CAPI=2:

name : ::butterfly
description: A butterfly module for a FFT

filesets:
  default:
    files:
      - mult.vhd
      - butterfly.vhd
    depend:
      - shift_register

targets:
  default:
    toplevel : butterfly
    filesets: [default]
